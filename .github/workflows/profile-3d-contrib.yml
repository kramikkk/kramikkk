name: Update 3D Contribution Graph

on:
  schedule:
    - cron: "30 0 * * *" # Daily at 00:30 UTC
  workflow_dispatch:

concurrency:
  group: profile-assets-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  update-3d-contrib:
    runs-on: ubuntu-latest
    name: Generate monochrome 3D contribution graph
    steps:
      - uses: actions/checkout@v6.0.3

      - uses: yoshi389111/github-profile-3d-contrib@v0.9.3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          USERNAME: ${{ github.repository_owner }}
          SETTING_JSON: conf/github-profile-3d-contrib.json

      - name: Move graph into assets
        run: |
          mkdir -p assets
          mv profile-3d-contrib/profile-monochrome.svg assets/profile-monochrome.svg

      - name: Commit & Push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/profile-monochrome.svg
          if git diff --staged --quiet; then
            echo "No 3D contribution graph changes to commit"
          else
            git commit -m "Update 3D contribution graph"
            git pull --rebase origin main
            git push
          fi
